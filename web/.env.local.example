# Public API base URL used by checkout + notify forms
NEXT_PUBLIC_API_URL=http://localhost:4000

# Optional CDN/object-storage base URL for heavy assets
# Leave empty in local dev to serve from web/public/assets
NEXT_PUBLIC_ASSET_BASE_URL=
